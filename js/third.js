'use strict'
class BattleBoard{
  static createBoard() {
    let board = document.getElementById('boardGame');
    let boardBat = document.createElement('div')
    boardBat.getAttribute('id', 'board');
    boardBat.style.border = '1px solid black';
    boardBat.style.width = '50%';
    boardBat.style.height = '100%';
    board.appendChild(boardBat);
}
static create
}
BattleBoard.createBoard()